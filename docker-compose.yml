# ==============================================================================
# Threat Intelligence Lab - single-node Wazuh SIEM + Linux honeypot agent
# Based on the official Wazuh 4.8 single-node deployment, extended with a
# containerised Ubuntu SSH honeypot that enrolls as a real Wazuh agent.
#
# Everything shares one Docker network so the honeypot agent can reach the
# manager on 1514/1515 by hostname. Run ./start_lab.sh (it generates the TLS
# certificates first, then brings this stack up).
# ==============================================================================

services:
  wazuh.manager:
    image: wazuh/wazuh-manager:4.8.0
    container_name: wazuh.manager
    hostname: wazuh.manager
    platform: linux/amd64   # Wazuh ships amd64 only; emulated on Apple Silicon
    restart: always
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 655360
        hard: 655360
    ports:
      - "1514:1514"   # agent -> manager (secure)
      - "1515:1515"   # agent enrollment (authd)
      - "514:514/udp" # syslog
      - "55000:55000" # Wazuh API
    environment:
      - INDEXER_URL=https://wazuh.indexer:9200
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - FILEBEAT_SSL_VERIFICATION_MODE=full
      - SSL_CERTIFICATE_AUTHORITIES=/etc/ssl/root-ca.pem
      - SSL_CERTIFICATE=/etc/ssl/filebeat.pem
      - SSL_KEY=/etc/ssl/filebeat.key
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
      # Consumed by scripts/malware_capture.py (the VirusTotal integration).
      # Put your real key in .env (see .env.example); defaults to a no-op.
      - VT_API_KEY=${VT_API_KEY:-YOUR_API_KEY_HERE}
    volumes:
      - wazuh_api_configuration:/var/ossec/api/configuration
      - wazuh_etc:/var/ossec/etc
      - wazuh_logs:/var/ossec/logs
      - wazuh_queue:/var/ossec/queue
      - wazuh_var_multigroups:/var/ossec/var/multigroups
      - wazuh_integrations:/var/ossec/integrations
      - wazuh_active_response:/var/ossec/active-response/bin
      - wazuh_agentless:/var/ossec/agentless
      - wazuh_wodles:/var/ossec/wodles
      - filebeat_etc:/etc/filebeat
      - filebeat_var:/var/lib/filebeat
      - ./config/wazuh_indexer_ssl_certs/root-ca-manager.pem:/etc/ssl/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.manager.pem:/etc/ssl/filebeat.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.manager-key.pem:/etc/ssl/filebeat.key
      - ./config/wazuh_cluster/wazuh_manager.conf:/wazuh-config-mount/etc/ossec.conf
      # Custom SOAR: VirusTotal lookup integration (runs on the manager).
      - ./scripts/malware_capture.py:/var/ossec/integrations/custom-virustotal:ro
      - ./scripts/malware_capture.py:/var/ossec/integrations/custom-virustotal.py:ro
      # Active-Response executable must also resolve on the manager (it validates
      # the command's binary before dispatching the order to the agent).
      - ./scripts/active_defense.py:/var/ossec/active-response/bin/active_defense.py:ro

  wazuh.indexer:
    image: wazuh/wazuh-indexer:4.8.0
    container_name: wazuh.indexer
    hostname: wazuh.indexer
    platform: linux/amd64   # Wazuh ships amd64 only; emulated on Apple Silicon
    restart: always
    ports:
      - "9200:9200"
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-indexer/certs/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.indexer-key.pem:/usr/share/wazuh-indexer/certs/wazuh.indexer.key
      - ./config/wazuh_indexer_ssl_certs/wazuh.indexer.pem:/usr/share/wazuh-indexer/certs/wazuh.indexer.pem
      - ./config/wazuh_indexer_ssl_certs/admin.pem:/usr/share/wazuh-indexer/certs/admin.pem
      - ./config/wazuh_indexer_ssl_certs/admin-key.pem:/usr/share/wazuh-indexer/certs/admin-key.pem
      - ./config/wazuh_indexer/wazuh.indexer.yml:/usr/share/wazuh-indexer/opensearch.yml
      - ./config/wazuh_indexer/internal_users.yml:/usr/share/wazuh-indexer/opensearch-security/internal_users.yml

  wazuh.dashboard:
    image: wazuh/wazuh-dashboard:4.8.0
    container_name: wazuh.dashboard
    hostname: wazuh.dashboard
    platform: linux/amd64   # Wazuh ships amd64 only; emulated on Apple Silicon
    restart: always
    ports:
      - "8443:5601"   # host 8443 -> dashboard (443 is a privileged/often-taken port)
    environment:
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - WAZUH_API_URL=https://wazuh.manager
      - DASHBOARD_USERNAME=kibanaserver
      - DASHBOARD_PASSWORD=kibanaserver
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - ./config/wazuh_indexer_ssl_certs/wazuh.dashboard.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.dashboard-key.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-dashboard/certs/root-ca.pem
      - ./config/wazuh_dashboard/opensearch_dashboards.yml:/usr/share/wazuh-dashboard/config/opensearch_dashboards.yml
      - ./config/wazuh_dashboard/wazuh.yml:/usr/share/wazuh-dashboard/data/wazuh/config/wazuh.yml
      - wazuh-dashboard-config:/usr/share/wazuh-dashboard/data/wazuh/config
      - wazuh-dashboard-custom:/usr/share/wazuh-dashboard/plugins/wazuh/public/assets/custom
    depends_on:
      - wazuh.indexer
    links:
      - wazuh.indexer:wazuh.indexer
      - wazuh.manager:wazuh.manager

  # --------------------------------------------------------------------------
  # Linux SSH honeypot. Real Ubuntu box (bash, useradd, rsyslog/auth.log) that
  # exposes a weak root:toor login on 2222 and ships its logs to Wazuh via a
  # baked-in agent. FIM watches /tmp, /home and /root for dropped payloads.
  # --------------------------------------------------------------------------
  linux-honeypot:
    build:
      context: .
      dockerfile: honeypot/Dockerfile
    image: threat-lab/linux-honeypot:latest
    container_name: linux-honeypot
    hostname: linux-honeypot
    restart: unless-stopped
    cap_add:
      - NET_ADMIN          # lets the Active Response script run iptables
    ports:
      - "2222:22"          # SSH honeypot target for the brute-force sim
    environment:
      - WAZUH_MANAGER=wazuh.manager
    depends_on:
      - wazuh.manager

volumes:
  wazuh_api_configuration:
  wazuh_etc:
  wazuh_logs:
  wazuh_queue:
  wazuh_var_multigroups:
  wazuh_integrations:
  wazuh_active_response:
  wazuh_agentless:
  wazuh_wodles:
  filebeat_etc:
  filebeat_var:
  wazuh-indexer-data:
  wazuh-dashboard-config:
  wazuh-dashboard-custom:
